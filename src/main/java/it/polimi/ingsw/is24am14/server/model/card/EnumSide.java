package it.polimi.ingsw.is24am14.server.model.card;

/**
 * This enum represents the side of a card.
 * Each Card has two sides: front and back, each with its own image and corners.
 */

public enum EnumSide {
    FRONT, BACK
}
